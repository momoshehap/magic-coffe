import 'package:flutter/material.dart';

class RewordScreen extends StatelessWidget {
  const RewordScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "Reword"
        ),
      ),
    );
  }
}
